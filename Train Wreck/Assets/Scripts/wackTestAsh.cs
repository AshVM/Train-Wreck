using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class wacktestash : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // this is a test to check if wackatime/hackatime is working!
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
